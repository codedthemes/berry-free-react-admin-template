export const DASHBOARD_PATH = '/finance/overview';
export const DEFAULT_THEME_MODE = 'system';

export const CSS_VAR_PREFIX = '';

const config = {
  fontFamily: `'Roboto', sans-serif`,
  borderRadius: 8
};

export default config;
